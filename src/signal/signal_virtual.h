#pragma once

bool signalDiff(
    const char* a,
    const char* b,
    float& out
);

bool signalSum(
    const char* a,
    const char* b,
    float& out
);

bool signalAnd(
    const char* a,
    const char* b,
    float& out
);
